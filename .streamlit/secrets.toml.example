# 이 파일을 secrets.toml 로 복사한 뒤 비밀번호를 변경하세요.
# 실제 secrets.toml 은 .gitignore 에 의해 GitHub에 올라가지 않습니다.
#
# Streamlit Cloud 배포 시:
#   1) 앱 페이지 → ⚙️ Settings → Secrets
#   2) 아래 내용을 그대로 붙여넣고 password 값을 변경
#   3) Save

[auth]
password = "여기에-원하는-비밀번호"
